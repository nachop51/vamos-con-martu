export interface Testimonial {
	name: string;
	trip: string;
	quote: string;
}

/**
 * Empty until there are real reviews — the Testimonios section
 * only renders when this array has items. Never add fake reviews.
 */
export const testimonials: Testimonial[] = [];
